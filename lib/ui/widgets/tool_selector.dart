import 'package:flutter/material.dart';

class ToolSelector extends StatelessWidget {
  const ToolSelector({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(); // TODO: Implement tool selection
  }
}